import { CustomerrorDirective } from './customerror.directive';

describe('CustomerrorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomerrorDirective();
    expect(directive).toBeTruthy();
  });
});
